<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicol.joynus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MainPageActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".CreateEventActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SearchEventActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".EditInterestActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".EventListActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".EventDescriptionActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".LocateActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBcd2n0_dcAHOK6umQHifCcox2zrrh9SNc" />

        <activity android:name=".ShowEventOnMapActivity"></activity>
    </application>

</manifest>